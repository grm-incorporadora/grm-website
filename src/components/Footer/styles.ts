import styled from 'styled-components';

export const ContainerCSS = styled.footer`
  text-transform: uppercase;

  width: 100%;

  color: rgb(var(--color-text900));
  background-color: rgb(var(--color-background100));

  > .bottom {
    text-align: center;
    padding: 1.5rem 0;

    color: rgb(var(--color-text100));
    background-color: rgb(var(--color-background900));
  }
`;

import styled from 'styled-components';
import { Menu } from '@styled-icons/boxicons-regular/Menu';

export const HamburgerMenuIcon = styled(Menu)`
  width: 2rem;
  height: 2rem;
  color: inherit;
`;
